# Python
__pycache__/
*.pyc
.venv/

# Node / Astro
site/node_modules/
site/dist/
site/.astro/

# SQLite sidecar files (the .db itself IS committed — data-in-git pattern)
data/*.db-wal
data/*.db-shm

# Pre-migration DB backups (kept locally, never committed)
data/evbus.db.pre_*.db

# Local scrape logs
data/*.log

# content-pipeline scratch virtualenv / caches (its source stays untracked for now)
content-pipeline/venv/
content-pipeline/__pycache__/

# Local scratch
digest.md
.DS_Store
